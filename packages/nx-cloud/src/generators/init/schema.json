{
  "$schema": "https://json-schema.org/schema",
  "version": 2,
  "title": "init generator",
  "description": "Registers @nx-devkit/nx-cloud in nx.json plugins so the nx-cloud-rotate target is inferred on the root project.",
  "type": "object",
  "properties": {
    "pluginPath": {
      "type": "string",
      "minLength": 1,
      "default": "@nx-devkit/nx-cloud",
      "description": "Plugin identifier written into nx.json plugins. Default: @nx-devkit/nx-cloud. Use a relative source path (e.g. ./packages/nx-cloud/src/plugin.ts) for in-repo development."
    }
  },
  "additionalProperties": false
}
